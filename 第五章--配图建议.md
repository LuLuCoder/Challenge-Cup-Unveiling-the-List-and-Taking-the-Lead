# 第五章配图建议（按当前《第五章--算法.docx》章节结构）

> 说明：图号按插入顺序连续编号；"数据来源"中标注 **软件运行结果** 的图，
> 可由仓库软件/脚本直接渲染生成，**自绘示意图** 需用绘图工具（Visio /
> matplotlib / PowerPoint）按文字描述绘制。统一建议：3D 图使用固定视角、
> 统一配色（高应力红橙、低应力蓝绿），图内文字不小于小五号，图题置于图下
> 居中，格式为"图 5-x 名称"。
>
> **已实现**：除界面截图（图5-10、图5-18）外的 16 张图已由
> `figures/make_figures.py` 自动生成，输出在 `figures/output/`，
> 详见 `figures/README.md`。

---

## 一、5.2 应力场驱动的连续纤维路径规划算法（建议 10 幅）

### 图 5-1 路径规划算法总体框架图

- **类型**：自绘流程图（横向 4 段或环形）。
- **内容**：应力场分析与方向场构建 → 层式路径生成 → 模板化与映射复用 →
  输出组织，四段之间标注输入/输出数据流（应力张量、主方向场、层式路径、
  标准化路径表）。
- **插入位置**：5.2.1 节末（算法框架段落之后）。

### 图 5-2 构件应力场与主应力方向可视化

- **类型**：软件运行结果（3D 节点云，两联图）。
- **内容**：(a) 节点云按 von Mises（或最大主应力）着色；(b) 局部区域叠加
  主应力方向箭头（quiver），展示应力分布与主方向的空间变化。
- **数据来源**：`data/node_coordinates_s.csv` + 六应力分量文件，用
  `ansys_path_planner` 的应力分析模块计算后渲染；建议选取零件应力集中
  区（孔边/圆角）放大。
- **插入位置**：5.2.2.2/5.2.2.3 之间（应力求解之后）。

### 图 5-3 方向场连续性处理前后对比

- **类型**：自绘示意图（左右两联）。
- **内容**：(a) 处理前：相邻单元主方向存在 90° 突变；(b) 处理后：经符号
  连续性化（2-RoSy 兼容）方向场平滑一致。用短箭头网格表达。
- **插入位置**：5.2.2.4（方向场符号歧义消除）。

### 图 5-4 应力自适应间距映射曲线

- **类型**：matplotlib 曲线图。
- **内容**：横轴归一化应力 $\bar\sigma\in[0,1]$，纵轴扫描间距
  $d(\bar\sigma)$，绘制 $\gamma=0.75$ 的幂函数曲线，标注 $d_{\max}$、
  $d_{\min}$ 与高应力/低应力区；可叠画不同 $\gamma$ 对比。
- **插入位置**：5.2.3.2（应力自适应间距映射）。

### 图 5-5 层式锯齿扫描路径生成示意

- **类型**：自绘示意图（两联）。
- **内容**：(a) 构件沿切片轴分层（每层不同颜色）；(b) 单层内分条 +
  相邻条交替方向的 zigzag 扫描线。
- **插入位置**：5.2.3.3（分层与层内锯齿扫描）。

### 图 5-6 空区检测与子路径切分示意

- **类型**：软件运行结果（带孔零件实际路径）。
- **内容**：3D 路径中"空区断开"段以特殊颜色/断裂标记，展示路径绕开
  孔洞、子路径切分效果；可叠加零件点云作背景。
- **数据来源**：零件1（带孔）数据运行路径规划，突出 `Segment_Type == "空区断开"` 的段。
- **插入位置**：5.2.3.4（空区检测与子路径切分）。

### 图 5-7 规划路径整体效果（密度着色）

- **类型**：软件运行结果（3D 路径）。
- **内容**：层式路径按密度等级（0–4）着色，图例置于图外，展示高应力区
  加密、低应力区稀疏的总体效果。
- **数据来源**：任意零件数据运行 `generate_layer_path` 后渲染
  `Density_Level`。
- **插入位置**：5.2.3.5（路径表语义）之后或 5.2.5 流程之后。

### 图 5-8 点云规范化与形状签名示意

- **类型**：软件运行结果/自绘示意（三联）。
- **内容**：(a) 原始点云（不同位姿、尺度）；(b) 规范坐标点云（质心归零 +
  主轴对齐 + 归一化）；(c) 主轴坐标直方图与径向距离直方图示例。
- **数据来源**：模板库 `geometry.csv` 与目标 STEP/节点点云。
- **插入位置**：5.2.4.1/5.2.4.2（点云规范化与形状签名）。

### 图 5-9 模板映射复用结果

- **类型**：软件运行结果（两联对比）。
- **内容**：(a) 模板零件路径与应力场；(b) 映射至目标零件后的路径与应力
  场（相似度标注在图注中，如 S=0.89）。验证映射后路径仍为层式结构且
  应力场连续。
- **数据来源**：零件1.STEP 与模板 test1 的映射流程输出。
- **插入位置**：5.2.4.3（应力场与路径映射）。

### 图 5-10 软件主界面与检查窗口截图

- **类型**：软件截图。
- **内容**：(a) 四画布主界面（几何 / 应力 / 路径 / 机械臂）；(b) STEP
  四视图检查窗口（模板形状、线框、四面体网格、点云）与网格质量报告。
- **插入位置**：5.2.6（软件实现情况）。

---

## 二、5.3 代理辅助的多目标工艺参数优化算法（建议 8 幅）

### 图 5-11 参数空间与正交布点示意

- **类型**：自绘示意图/热力图。
- **内容**：$3^4=81$ 组参数空间（2D 投影或网格热力图），标出 9 组
  正交实验点的位置，说明小样本对空间的代表性覆盖。
- **插入位置**：5.3.1（问题定义与数据基础）。

### 图 5-12 GPR 代理模型留一法验证散点图

- **类型**：软件运行结果（1×3 子图）。
- **内容**：三个目标（ΔT/ΔB/ΔS）各自的 LOO 预测值 vs 实测值散点，叠加
  45° 参考线，标注 MAE / RMSE / R²。
- **数据来源**：`satc_optimizer` 的 `loo_validation` 输出。
- **插入位置**：5.3.2.3（留一法验证）。

### 图 5-13 GPR 预测不确定性与一维扫描示意

- **类型**：自绘示意图（matplotlib）。
- **内容**：固定其余参数，扫描某一参数（如打印速度 D），绘制三目标预测
  均值曲线及 ±2σ 置信带，直观展示代理模型的预测与不确定性。
- **插入位置**：5.3.2.2（数值实现）之后。

### 图 5-14 81 组全空间预测结果图

- **类型**：软件运行结果（散点矩阵或平行坐标图）。
- **内容**：81 组方案的三目标取值（真实点与预测点用不同形状/颜色区分），
  展示目标间权衡与数据来源分布。
- **数据来源**：`SATC_NS_all_predictions.csv`。
- **插入位置**：5.3.3（参数空间构造与目标值组织）。

### 图 5-15 Pareto 前沿与推荐方案

- **类型**：软件运行结果（3D 散点，可加 2D 投影子图）。
- **内容**：81 组解全部绘制（浅色），Pareto 前沿高亮（深色），标注推荐
  方案（红三角）与论文基准方案（蓝方块），附理想点箭头；图注写明推荐
  方案参数与综合评分。
- **数据来源**：`SATC_NS_Pareto_results.csv` + 摘要。
- **插入位置**：5.3.4.3（加权折中评分与百分制）之后。

### 图 5-16 折中评分与权重敏感性

- **类型**：软件运行结果/自绘（可选）。
- **内容**：不同权重组合（等权 / 强度优先 / 效率优先）下推荐方案在
  Pareto 前沿上的位置变化，说明权重对推荐结果的影响。
- **插入位置**：5.3.4.3 或 5.3.5 之后。

### 图 5-17 力学暴露度与自动权重

- **类型**：软件运行结果（柱状图 + 饼图/条形图）。
- **内容**：三类失效暴露度（拉伸 / 弯曲 / 层间剪切）柱状图，及归一化
  权重条形图；可在图注中给出权重数值与依据说明。
- **数据来源**：`satc_optimizer` 的 `suggest_weights` 输出（节点坐标 +
  六应力分量 + 变形文件）。
- **插入位置**：5.3.5（力学证据驱动的目标权重确定）。

### 图 5-18 参数优化软件界面截图

- **类型**：软件截图。
- **内容**：结果摘要 / LOO 验证 / 81 组预测 / Pareto 前沿 / 可视化 /
  运行日志标签页（可并排 2–3 张）。
- **插入位置**：5.3.7（软件实现情况）。

---

## 三、绘图执行要点

1. **统一风格**：所有 3D 图固定视角（如仰角 25°、方位角 -60°），同一色系
   表示同一物理量；图内字体不小于 8 pt，图注不小于 10.5 pt。
2. **数据来源标注**：软件运行结果图在图注中注明"软件运行结果"；自绘示意
   图注明"示意图"。
3. **分辨率**：插入 Word 的图片建议 300 dpi 以上，单图宽度 12–14 cm，
   两联图每幅 8–10 cm。
4. **可复用脚本**：图 5-2、5-6、5-7、5-9 可由
   `ansys_path_planner` 数据直接渲染；图 5-12、5-14、5-15、5-17 可由
   `satc_optimizer` 结果 CSV 直接渲染，可后续统一写成 `make_figures.py`
   一键出图。
