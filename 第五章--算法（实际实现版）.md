# 第五章 连续纤维复合材料增材制造智能算法

连续纤维增强聚合物（cCFRP）增材制造将连续纤维束与热塑性基体同步沉积，
其构件的力学性能高度依赖于纤维铺放方向与外部载荷路径之间的协调程度。
与传统各向同性材料的填充策略不同，连续纤维的承载能力具有显著的取向性，
若铺放方向与主应力方向偏离过大，纤维将无法充分发挥增强作用，导致构件
在承载方向上强度冗余、在受力方向上强度不足，形成典型的各向异性失配问题。
因此，打印路径的生成不能仅以几何填充密度为目标，而必须以构件在服役
载荷下的应力场为依据，使纤维取向与应力传递路径保持一致。与此同时，
工艺参数的设定同样直接影响成型质量：层厚、喷嘴温度与打印速度等参数之间
存在强耦合的非线性交互，单因素轮换法难以在有限试验成本内找到全局较优
组合，需要借助代理模型与多目标寻优技术在参数空间中系统搜索。

针对上述问题，本章给出两套核心算法：其一为**应力场驱动的连续纤维路径
规划算法**（5.2 节），将有限元应力结果转化为面向 3D 打印的层式纤维路径，
并通过模板映射机制支持系列化零件的快速复用；其二为**代理辅助的多目标
工艺参数优化算法**（5.3 节），以小样本真实实验数据训练高斯过程代理模型，
在完整参数空间内求取满足热约束的 Pareto 最优工艺配置。两套算法共享同一
套 ANSYS 力学数据接口，前者为后者提供应力场层面的力学证据，后者为前者
提供工艺参数层面的可行性保障，共同构成"仿真—设计—工艺"的闭环。为便于
区分算法本身与其工程载体，本章各节先阐述算法原理与实现流程，再集中说明
配套软件的实现情况。

---

# 5.2 应力场驱动的连续纤维路径规划算法

## 5.2.1 问题描述与算法框架

连续纤维复合材料的打印路径本质上是纤维取向的工程表达。等距填充等传统
策略以几何规则确定扫描方向，未考虑载荷分布，因而在复杂应力状态下难以
保证纤维沿主应力方向铺放；单纯的主应力流线追踪虽能贴合应力方向，却存在
方向歧义、路径密度不可控以及逐件重复计算等缺陷。本文将二者统一于应力
驱动框架之下：以主应力方向场引导路径走向，以主应力幅值调节路径密度，
使路径在力学上与结构承载需求相匹配。

在工程应用中，同类构件常以系列化形式出现，即拓扑构型相同而几何尺寸与
局部特征不同。若对每个规格独立开展有限元分析与全场路径优化，将造成显著
的计算冗余与工艺准备延迟。为此，本文引入模板映射复用机制：对基准构件
一次生成标准路径模板并入库，对同类型变尺寸零件先评判其与模板的形状
相似度，相似度达标后直接将模板应力场与路径映射至目标几何，从而将系列化
零件的路径准备时间压缩至秒级。

基于上述思想，算法整体划分为四个环节：第一，应力场分析与方向场构建，
由六个应力分量重建应力张量，批量求解主应力、主方向与 von Mises 应力，
并消除方向场的符号歧义；第二，层式路径生成，沿切片轴分层，在层内以
应力自适应间距生成锯齿扫描路径，并对穿越无实体区域的路径段实施空区检测
与子路径切分；第三，模板化与映射复用，通过形状签名评判相似度，在相似度
达标时完成应力场与路径的映射传递；第四，输出组织，生成携带层号、段类型、
子路径编号与优先级等语义信息的标准化路径表，供后续执行与可视化使用。
各环节的输入输出相互衔接，构成完整的算法链路。

## 5.2.2 应力场提取与方向场构建

### 5.2.2.1 多文件数据融合

有限元分析结果通常以节点坐标文件与各应力分量文件分别输出。算法首先对
节点坐标文件与六个应力分量文件（$X$、$Y$、$Z$、$XY$、$YZ$、$XZ$）执行
自动识别与分类，依据文件名语义和文件内容特征判定文件类型，再以节点号
（`Node`）为关联键完成内连接融合。该融合过程对文件中可能存在的混合分隔
符、科学计数法、异常表头与缺失行等格式噪声具有鲁棒性，保证后续应力
分析建立在完整、对齐的节点数据之上。

### 5.2.2.2 应力张量重建与主应力求解

对每个节点，六个应力分量 $S_X,S_Y,S_Z,S_{XY},S_{YZ},S_{XZ}$ 组装为对称
应力张量

$$
\boldsymbol{\sigma}=
\begin{bmatrix}
S_X & S_{XY} & S_{XZ}\\
S_{XY} & S_Y & S_{YZ}\\
S_{XZ} & S_{YZ} & S_Z
\end{bmatrix}.
$$

应力张量的主应力即为该点处剪应力为零的截面上的正应力，反映材料在该点
承受的最大拉伸、中间与最大压缩载荷水平。算法对全部节点的应力张量执行
批量对称特征分解，一次性求得主应力 $\sigma_1\ge\sigma_2\ge\sigma_3$ 及
对应的主方向 $\boldsymbol{v}_1,\boldsymbol{v}_2,\boldsymbol{v}_3$。批量
特征分解将逐节点的循环计算转化为一次矩阵运算，在十余万节点规模下仍能
在数秒内完成，兼顾了数值精度与计算效率。

### 5.2.2.3 von Mises 等效应力

除主应力外，算法还计算 von Mises 等效应力，用于综合表征多轴应力状态
下的材料屈服倾向。von Mises 应力由应力偏量第二不变量定义，其表达式为

$$
\sigma_{\mathrm{vm}}=\sqrt{\tfrac{1}{2}\big[
(\sigma_x-\sigma_y)^2+(\sigma_y-\sigma_z)^2+(\sigma_z-\sigma_x)^2
+6(\tau_{xy}^2+\tau_{yz}^2+\tau_{xz}^2)\big]}.
$$

该指标将复杂的六分量应力状态压缩为单一标量，物理意义清晰，适合作为
路径密度与优先级划分的力学依据：应力水平高的区域路径应加密，应力水平低
的区域可适当稀疏。

### 5.2.2.4 方向场符号歧义消除

主应力方向由特征向量给出，但特征向量与其反向在物理上完全等价，数值上
却可能造成相邻节点方向突变，进而导致路径方向场的剧烈跳变。为消除该
歧义，算法借鉴 2-RoSy（二重旋转对称）方向场兼容化思想：沿节点遍历顺序
执行方向连续性化，若相邻节点主方向的内积为负，则翻转其中一个方向，使
方向场在整体上保持平滑。该处理既保留了主方向的物理信息，又为后续路径
方向引导与映射后的方向校验提供了连续一致的方向场输入。

## 5.2.3 应力自适应层式路径生成算法

### 5.2.3.1 应力归一化与优先级划分

不同构件的应力量级差异显著，为使间距映射与优先级划分具有尺度无关性，
算法首先对最大主应力 $\sigma_1$ 执行 Min-Max 归一化，得到
$\bar\sigma\in[0,1]$。随后以分位数 $\tau$（默认第 75 百分位）为阈值，将
节点划分为高应力优先区与常规覆盖区：高于阈值的节点标记为高优先级，在
路径生成中赋予更小的扫描间距与更高的路径权重；低于阈值的节点仍全部参与
规划，仅获得较低的优先级权重。这一设计保证了路径对构件的全覆盖，避免
因应力筛选而遗漏低应力区域的纤维增强。

### 5.2.3.2 应力自适应间距映射

路径扫描间距是控制纤维密度的核心参数。算法建立归一化应力与扫描间距之间
的幂函数映射

$$
d(\bar\sigma)=d_{\max}-(d_{\max}-d_{\min})\,\bar\sigma^{\gamma},
$$

其中 $d_{\max}=0.045\,L_c$、$d_{\min}=0.018\,L_c$，$L_c$ 为模型包围盒
的特征长度，$\gamma=0.75$ 为间距—应力权衡指数。该映射具有如下性质：当
应力为零时间距取最大值 $d_{\max}$，当应力归一化值为 1 时间距取最小值
$d_{\min}$，中间应力水平按幂函数平滑过渡。指数 $\gamma<1$ 使得中低应力
区间距下降较快、高应力区间距变化趋于平缓，从而在保证高应力区足够致密的
同时避免低应力区过度稀疏。用户亦可指定基准间距，此时间距在基准值及其
倍数之间变化，便于针对不同构件尺度与工艺约束进行调整。

与间距映射相配套，路径优先级权重定义为

$$
W=0.25+0.75\,\bar\sigma,
$$

即权重随应力线性增长。低应力区域的路径不因应力低而被删除，而是以较低的
权重参与排序与绘制，从而实现力学性能与材料效率的兼顾：高应力区获得致密
增强，低应力区保持完整覆盖而不浪费过多材料。

### 5.2.3.3 分层与层内锯齿扫描

面向 3D 打印的逐层沉积工艺，算法沿切片轴（默认 $Z$）将节点云均分为
$n$ 层（默认 20 层），层厚由构件高度与分层数共同确定。每一层内，算法沿
分条轴以局部自适应间距 $d(\bar\sigma)$ 划分条带，条带内节点沿扫描轴排序，
相邻条带的扫描方向交替反转，形成连续锯齿（zigzag）扫描线。交替方向设计
使机械臂或打印头在条带端点处无需大幅回程即可连续运动，减少空行程与急停
转向。各层按顺序连接，最终形成一条覆盖全模型的连续路径，层与层之间以
层间过渡段衔接。

### 5.2.3.4 空区检测与子路径切分

构件中孔洞、凹槽等无实体区域对应的节点云存在空缺，若路径段直接穿越这些
区域，将导致纤维悬空铺设。算法沿路径段均匀采样，利用 KD 树查询采样点
邻域内的最近节点距离：若最近节点距离超过检测半径，说明该处不存在零件
实体，路径段被标记为"空区断开"并切分为独立子路径。检测半径的设计兼顾
网格密度差异：

$$
r_v=\max(0.045\,L_c,\ d_{\max},\ \bar d_{\mathrm{nn}}),
$$

其中 $\bar d_{\mathrm{nn}}$ 为节点云的中位最近邻间距。对于粗网格，中位
最近邻间距较大，检测半径自动放大，避免将正常稀疏区域误判为空区；对于
细网格，检测半径主要由特征长度与最大间距决定，保证空区识别的灵敏度。
在实现上，全部路径段的采样点被批量合并为一次 KD 查询，避免了逐段查询的
重复开销，使空区判定在十余万节点规模下仍保持毫秒级。

### 5.2.3.5 路径表语义

算法输出携带完整语义的路径表，各字段定义如下，供执行层与可视化层直接
使用：

**表 5-1 路径输出字段定义**

| 字段 | 含义 | 取值说明 |
| --- | --- | --- |
| `Step` | 路径点序号 | 全局连续编号，标识路径点顺序 |
| `Layer` | 层号 | 沿切片轴自下而上递增，标识所属打印层 |
| `Node / X,Y,Z` | 节点号与坐标 | 构件坐标系下的位置，单位 mm |
| `Segment_Type` | 段类型 | 层内路径 / 层间过渡 / 空区断开 |
| `SubPath` | 子路径编号 | 按空区断开切分的独立子路径序号 |
| `Path_Spacing` | 局部扫描间距 | 由应力自适应间距映射确定 |
| `Density / Density_Level` | 密度与等级（0–4） | 供路径密度着色与统计 |
| `Path_Weight` | 优先级权重 | $W=0.25+0.75\bar\sigma$ |

其中 `Segment_Type` 与 `SubPath` 字段实现了路径的结构化组织：空区断开
段不参与打印，仅作为子路径边界标记，保证实际执行路径始终位于实体内部。

## 5.2.4 形状签名与模板映射算法

### 5.2.4.1 点云规范化

不同零件的点云在空间位姿、尺度与密度上均存在差异，直接比较原始坐标
无法反映形状的本质。算法首先对点云执行规范化：将质心平移至原点，对
协方差矩阵特征分解并按特征值降序取主轴作为坐标基，通过"该轴坐标和为正"
的约束消除主轴符号歧义，最后以包围盒最长边将坐标等比例缩放至单位尺度。
规范化后，点云的比较仅取决于形状本身，而与平移、旋转、镜像和缩放无关，
为后续签名统计与模板映射提供了统一坐标系。

### 5.2.4.2 形状签名与相似度评判

在规范坐标系下，算法统计三个主轴方向的坐标直方图（16 箱）与径向距离
直方图（24 箱），以直方图分布而非逐点对应关系描述形状，从而对点云密度
差异具有鲁棒性。两个直方图之间的相似度采用归一化 $L_1$ 距离度量。为消除
主轴排序与方向翻转带来的不确定性，对主轴直方图执行 $6$ 种轴置换与 $8$
种符号翻转的组合最优匹配，取最大相似度作为该组直方图的匹配结果。此外，
算法引入三个旋转不变量直方图——3D 径向距离、面内半径以及沿最小特征值轴
的厚度分布——用于处理近对称零件主轴方向不稳定的情况。综合相似度由
不变量直方图相似度与主轴直方图相似度加权融合：

$$
S=0.65\,S_{\mathrm{inv}}+0.35\,S_{\mathrm{axis}},
$$

当综合相似度达到阈值（默认 0.80）时，判定目标零件与模板在拓扑构型上
一致，允许进入映射复用流程。

### 5.2.4.3 应力场与路径映射

相似度达标后，算法在规范坐标系内完成应力场与路径的映射。模板节点云与其
目标节点云均经各自的规范变换投影到同一坐标系，模板应力场以 $k$ 近邻
（默认 3 邻）反距离平方加权插值到目标节点：

$$
\tilde f(\boldsymbol{x}_j)=\frac{\sum_{i\in\mathcal N_j}
f(\boldsymbol{x}_i)\,w_{ij}}{\sum_{i\in\mathcal N_j}w_{ij}},\qquad
w_{ij}=\frac{1}{(d_{ij}^2+\epsilon)^2},
$$

其中 $d_{ij}$ 为规范空间内目标节点与模板近邻节点间的欧氏距离，$\epsilon$
为防止除零的极小正则项。反距离平方加权使邻近模板节点的应力值对目标节点
的贡献更大，插值结果在几何相似区域内平滑且连续。模板路径点则经规范坐标
变换传递至目标坐标系，随后吸附至最近目标节点并同步携带应力值，以保证
路径与节点数据的一致对应。映射得到的应力场与路径可进一步作为层式规划器
的输入，在目标几何上重新生成分层路径，从而保证输出路径仍为层式打印结构。
该映射过程仅涉及矩阵变换与近邻查询，计算复杂度低，可实现系列化零件的
秒级路径复用。

## 5.2.5 算法整体流程

综合上述环节，应力场驱动的路径规划算法按以下步骤执行：

1. **数据输入与融合**。读取节点坐标与六个应力分量，按节点号内连接融合；
   若输入为 STEP 几何，则先完成网格化并提取节点点云，再按后续步骤处理。
2. **应力场分析**。重建各节点应力张量，批量特征分解求主应力与主方向，
   计算 von Mises 应力，并对方向场执行符号连续性化。
3. **归一化与优先级划分**。对最大主应力 Min-Max 归一化，按分位数阈值
   划分高应力优先区与常规覆盖区，计算自适应间距场与路径权重场。
4. **分层与层内扫描**。沿切片轴均分层，层内按自适应间距分条并生成交替
   方向的锯齿扫描线，层间顺序连接为连续路径。
5. **空区检测与子路径切分**。沿路径段采样并查询最近节点距离，将穿越
   无实体区域的段标记为"空区断开"，切分为独立子路径。
6. **模板匹配与映射**。计算目标零件形状签名并与模板库比对；相似度达标
   时映射模板应力场与路径并在目标几何上重新分层规划，否则直接使用当前
   数据生成的路径。
7. **路径输出**。生成携带层号、段类型、子路径编号、间距与优先级等语义
   字段的标准化路径表，交付可视化与执行环节。

各步骤之间存在明确的数据依赖关系，前一环节的输出即为后一环节的输入，
保证了算法流程的模块化与可复现性。

## 5.2.6 软件实现情况

### 5.2.6.1 数据接入

软件支持两种数据接入方式。其一为多文件导入：一次多选节点坐标文件与六个
应力分量文件，程序按文件名与内容自动识别分类，兼容常见 ANSYS 导出格式
中的混合分隔符、科学计数法与异常表头。其二为 STEP 几何直接导入：对
`.step/.stp` 文件调用 gmsh（OpenCASCADE 内核）自动生成四面体网格，以网格
节点作为点云。网格质量以 minSICN（最小缩放雅可比，正四面体取 1）自动
评判，统计平均质量、最差质量、差单元占比与实体体积，并给出"优秀/良好/
需细化"的判定结论；软件同时提供四视图检查窗口，分别展示模板形状、几何
线框、四面体网格与节点点云，供操作者核验数据源头。

### 5.2.6.2 模块划分

应力分析、层式规划、形状签名、模板库与映射分别在独立模块中实现，模块
间通过标准化数据结构交互。模板库以"节点坐标与应力场、路径表、元数据"
的形式持久化，元数据记录形状签名与规范变换参数，使模板可跨会话复用。

### 5.2.6.3 可视化与交互

软件提供四个独立 3D 画布，分别展示模型几何、应力分析、规划路径与虚拟
机械臂。机械臂画布以六自由度关节臂模型实时演示末端沿路径的运动，直观
反映路径的可执行性；画布支持双击放大与滚动查看，节点云可全量显示。

### 5.2.6.4 性能

数据导入与路径生成在后台线程执行，界面保持流畅。实测 13.8 万节点数据
导入约 2 秒、路径规划约 2 秒；STEP 几何导入约 0.3 秒即可完成网格化与
质量评判，满足交互式工艺准备的需求。

---

# 5.3 代理辅助的多目标工艺参数优化算法

## 5.3.1 问题定义与数据基础

连续纤维复合材料的成型质量受层厚、首层层厚、喷嘴温度与打印速度等多个
工艺参数的共同影响，且参数之间存在显著的非线性交互。例如，喷嘴温度既
决定基体熔融程度，又与打印速度共同决定层间热历史；层厚与首层层厚则直接
影响纤维体积分数与层间结合强度。若采用单因素轮换法逐一调试，不仅试验
量庞大，而且无法捕捉参数间的协同效应，难以获得全局较优的工艺配置。

算法以四项工艺参数为决策变量，各取三个水平，构成 $3^4=81$ 组离散参数
空间：

**表 5-2 决策变量与水平**

| 参数 | 符号 | 单位 | 水平 |
| --- | --- | --- | --- |
| 层厚 | $A$ | mm | 0.15 / 0.20 / 0.25 |
| 首层层厚 | $B$ | mm | 0.15 / 0.20 / 0.25 |
| 喷嘴温度 | $C$ | °C | 180 / 200 / 220 |
| 打印速度 | $D$ | mm/s | 35 / 40 / 45 |

评价指标为三个越小越好的目标：**ΔT**（拉伸偏差）反映拉伸试验的尺寸偏差
与强度一致性，**ΔB**（弯曲偏差）反映弯曲刚度与层间结合质量，**ΔS**
（层间剪切偏差，ILSS）反映抗分层能力。数据来源为 9 组真实实验，按
$L_9(3^4)$ 正交表布点，以较少的试验覆盖参数空间的主要组合。工艺可行性
由热约束界定：喷嘴温度须满足 $C\in[180,\,220]\,^\circ\mathrm{C}$，超出
该范围视为工艺不可行。

## 5.3.2 高斯过程代理模型

### 5.3.2.1 模型原理

高斯过程回归将未知目标函数视为高斯过程先验，通过观测数据更新其后验
分布。对于任意输入 $\boldsymbol{x}$，其预测均值与方差均可由训练数据与
核函数解析给出，因此在小样本条件下既能提供预测值，又能量化预测不确定
性。算法采用 RBF 核

$$
k(\boldsymbol{x},\boldsymbol{x}')=
\exp\!\Big(-\frac{\|\boldsymbol{x}-\boldsymbol{x}'\|^2}{2\ell^2}\Big),
$$

该核函数度量输入点之间的相似度，长度尺度 $\ell$ 控制相似度随距离衰减的
速率。对三个优化目标分别训练独立的代理模型，每个模型以全部 9 组真实
实验数据为训练集，输入经标准化处理以消除量纲差异。

### 5.3.2.2 数值实现

预测过程需要对训练协方差矩阵
$\boldsymbol{K}+\sigma_n^2\boldsymbol{I}$ 求逆。为避免显式求逆带来的数值
不稳定与计算开销，算法采用 Cholesky 分解，通过两次三角回代求解
$\boldsymbol{\alpha}=(\boldsymbol{K}+\sigma_n^2\boldsymbol{I})^{-1}
\boldsymbol{y}$，再以 $\boldsymbol{\alpha}$ 与核向量完成预测。当协方差
矩阵接近奇异时，算法逐次增大 jitter 项并重新分解，保证数值稳定性。预测
标准差由后验方差公式给出，反映了代理模型对预测值的置信程度。

### 5.3.2.3 留一法验证

为定量评估代理模型的泛化能力，算法执行留一法（LOO）交叉验证：对 9 组
数据逐一样本留一训练并预测，累计得到全部样本的预测值，进而计算三个目标
的 MAE（平均绝对误差）、RMSE（均方根误差）与 R²（决定系数）。MAE 反映
预测偏差的平均水平，RMSE 对大误差更为敏感，R² 反映模型对目标变异的解释
能力。验证通过后，使用全部 9 组数据训练最终模型，用于后续参数空间预测。

## 5.3.3 参数空间构造与目标值组织

对 $3^4=81$ 组参数组合，最终代理模型输出三目标预测均值与标准差。为
保证优化结果的可信度，目标值组织遵循"真实优先"原则：9 个已实验点采用
真实目标值，其余 72 个未实验点采用代理预测值，并在结果中显式标注每个
方案的数据来源。该原则确保优化过程中不将预测结果冒充真实实验数据，
使 Pareto 前沿与推荐方案均建立在可追溯的数据基础之上。

## 5.3.4 约束 Pareto 寻优与折中推荐

### 5.3.4.1 支配关系与 Pareto 前沿

多目标优化中不存在单一最优解，而是存在一组互不支配的权衡解。解
$\boldsymbol{f}_1$ 支配 $\boldsymbol{f}_2$ 当且仅当 $\boldsymbol{f}_1$
在各目标上均不劣于 $\boldsymbol{f}_2$，且至少在一个目标上严格更优。所有
不被其他解支配的解构成 Pareto 前沿，前沿上的解在强度、精度与效率之间
具有不同的权衡倾向，均可能成为工程推荐对象。

### 5.3.4.2 热约束处理

在计算前沿之前，算法先对 81 组解执行热约束过滤：喷嘴温度超出
$[180,220]\,^\circ\mathrm{C}$ 的方案视为不可行，不参与前沿计算。若全部
方案均违反热约束，则退回违规量最小的 10 组作为候选，保证算法在极端
情况下仍有输出。

### 5.3.4.3 加权折中评分与百分制

为从 Pareto 前沿中给出工程可用的单一推荐，算法按用户权重对各目标归一化
加权：

$$
\mathrm{score}(\boldsymbol{f})=
\sum_i w_i\,\frac{f_i-f_i^{\min}}{f_i^{\max}-f_i^{\min}},\qquad
\sum_i w_i=1,
$$

其中 $f_i^{\min}$ 与 $f_i^{\max}$ 取前沿上各目标的极值，归一化后各目标
位于 $[0,1]$，加权平均分越小表示方案越接近理想点。为便于工程解读，评分
统一转换为百分制 $\mathrm{score}_{100}=100\,(1-\mathrm{score})$，100 分
最好、0 分最差。基准方案（如论文最优方案）使用与前沿解相同的归一化基准
计算评分，保证不同方案之间具有可比性。

### 5.3.4.4 方案对比与可信度声明

算法输出推荐方案与基准方案的参数、三目标预测值及预测标准差，并判定二者
的 Pareto 支配关系。需要强调的是，推荐方案的目标值若来自代理预测，其
数值具有不确定性，必须以真实实验验证后方可采信；预测标准差与 LOO 指标
共同为该不确定性提供量化依据。

## 5.3.5 力学证据驱动的目标权重确定

三个目标权重的物理意义分别为：拉伸偏差权重对应最大主应力正分量，反映
拉应力水平；弯曲偏差权重对应挠度或弯曲变形水平；层间剪切偏差权重对应
层间剪应力水平。为使权重设定从经验赋值升级为力学证据驱动，算法定义三类
失效风险暴露度

$$
e=\mathrm{mean}\!\big((v/v_{\max})^2\big),
$$

该指标以归一化值的平方均值刻画高应力区的加权占比，突出失效风险集中程度：

- **拉伸暴露度**：取最大主应力 $\sigma_1$ 的正值部分，正值代表拉应力，
  直接对应拉伸失效风险；
- **弯曲暴露度**：取总变形量；缺少变形数据时，以沿打印方向各层平均
  $\sigma_1$ 的变化幅度近似表征弯曲变形；
- **层间剪切暴露度**：取层间剪应力 $\sqrt{\tau_{xz}^2+\tau_{yz}^2}$，
  反映层间剪切失效风险。

最终权重为三类暴露度的归一化结果；若某类指标因数据缺失无法计算，则以
已计算指标的均值作为基准，避免权重退化。该机制使权重设定与力学仿真结果
直接关联，与 5.2 节的应力场分析共享同一套数据接口。

## 5.3.6 算法整体流程

工艺参数优化算法按以下步骤执行：

1. **权重校验与归一化**。校验用户输入权重的数量与数值有效性，缺省时
   使用等权，并将权重归一化使之和为 1。
2. **数据完整性校验**。检查 9 组真实实验数据的参数维度与目标维度是否
   满足要求。
3. **留一法验证**。对 9 组数据逐样本留一训练并预测，输出三个目标的
   MAE、RMSE 与 R²，作为代理模型可信度的定量依据。
4. **最终模型训练**。使用全部 9 组数据训练三目标 GPR 代理模型。
5. **全空间预测**。生成 $3^4=81$ 组参数组合，由代理模型输出各组合的三
   目标预测均值与标准差。
6. **目标值组织**。按"真实优先"原则，以真实值替换 9 个已实验点的预测值，
   并标注数据来源。
7. **约束 Pareto 求取**。执行热约束过滤，在可行域内求取非支配前沿；无
   可行解时退回违规量最小的 10 组。
8. **折中推荐**。按权重在归一化基准上计算前沿解的加权评分，推荐评分
   最优的方案。
9. **对比与输出**。与基准方案同基准对比，输出摘要与全过程结果（留一
   验证表、81 组预测表、Pareto 表、摘要表）。

上述流程中，代理模型评估替代了物理实验的重复执行，使 81 组参数空间的
评价在毫秒级完成，而留一验证与预测标准差则为结果的可信度提供了量化保障。

## 5.3.7 软件实现情况

### 5.3.7.1 模块划分

代理模型、留一验证、Pareto 与评分、权重计算分别在独立模块中实现，优化
主流程按 5.3.6 的步骤编排，各环节的输出以 CSV 文件持久化，包括留一验证
表、81 组全空间预测表、Pareto 前沿表与最终摘要表。

### 5.3.7.2 数据接口

权重设定支持手工输入与力学自动计算两种方式。力学自动权重直接读取与路径
规划共用的 ANSYS 文件格式（节点坐标、六应力分量、可选变形结果），一次
多选自动分类后计算三类暴露度，并给出权重取值与依据说明。

### 5.3.7.3 界面与输出

图形界面以标签页展示结果摘要、留一验证指标、81 组预测表、Pareto 前沿
表、可视化图表与运行日志；优化过程在后台线程执行，界面保持响应。由于
样本规模有限，代理预测存在不确定性，软件以 LOO 指标与预测标准差显式
量化该不确定性，并将"预测目标值需实验验证"作为推荐方案的前置声明。

---

## 本章代码映射

| 章节 | 仓库模块 | 关键文件/函数 |
| --- | --- | --- |
| 5.2 | `ansys_path_planner` | `analysis/stress.py`（应力张量/主应力）、`analysis/path_planning.py`（层式规划/空区检测）、`analysis/shape_signature.py`（形状签名）、`analysis/template_library.py`（模板库/映射）、`parsers/step_mesh.py`（gmsh 网格）、`visualization/virtual_arm.py`（机械臂） |
| 5.3 | `satc_optimizer` | `satc/gpr.py`（GPR 代理）、`satc/validation.py`（LOO）、`satc/pareto.py`（Pareto/评分）、`satc/pipeline.py`（主流程）、`satc/mechanics.py`（力学自动权重）、`satc/config.py`（数据/水平） |
